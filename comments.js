 // create web server 
 // 1. load module http
 var http = require("http");
 // 2. create web server
 var server = http.createServer(function(request, response){
     // 3. set header
     response.writeHead(200, {"Content-Type": "text/plain"});
     // 4. write message
     response.end("Hello World");
 });
 // 5. listen port
 server.listen(3000);
 console.log("Server running at http://localhost:3000/");

 // 6. run in terminal
 // node comments.js

 // 7. open browser and access http://localhost:3000/

 // 8. ctrl + c to stop server

 // Path: comments.js
 // create web server 
 // 1. load module http
 var http = require("http");
 // 2. create web server
 var server = http.createServer(function(request, response){
     // 3. set header
     response.writeHead(200, {"Content-Type": "text/plain"});
     // 4. write message
     response.end("Hello World");
 });
 // 5. listen port
 server.listen(3000);
 console.log("Server running at http://localhost:3000/");

 // 6. run in terminal
 // node comments.js

 // 7. open browser and access http://localhost:3000/

 // 8. ctrl + c to stop server

 // Path: comments.js
 // create web server 
 // 1. load module http
 var http = require("http");
 // 2. create web server
 var server = http.createServer(function(request, response){
     // 3. set header
     response.writeHead(200, {"Content-Type": "text/plain"});
     // 4. write message
     response.end("Hello World");
 });
 // 5. listen port
 server.listen(3000);
 console.log("Server running at http://localhost:3000/");

 // 6. run in terminal
 // node comments.js

 // 7. open browser and access http://localhost:3000/

 // 8. ctrl + c to stop server

 // Path: comments.js
 // create web server 
 // 1. load module http
 var http = require("http");
 // 2. create web server
 var server = http.createServer(function(request, response){
     // 3. set header
     response.writeHead(200, {"Content-Type": "text/plain"});
